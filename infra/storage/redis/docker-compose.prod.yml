services:
  redis:
    image: redis:7-alpine
    container_name: redis
    restart: always
    command:
      - "redis-server"
      - "--appendonly"
      - "yes"
      - "--maxmemory"
      - "512mb"
      - "--maxmemory-policy"
      - "allkeys-lru"
    volumes:
      - redis_data:/data
    expose:
      - "6379"
    networks:
      - traefik-net

volumes:
  redis_data:

networks:
  traefik-net:
    name: traefik-net
    external: true
