services:
  redis:
    image: redis:7-alpine
    container_name: redis-dev
    restart: always
    command:
      - "redis-server"
      - "--appendonly"
      - "yes"
      - "--maxmemory"
      - "256mb"
      - "--maxmemory-policy"
      - "allkeys-lru"
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    networks:
      - traefik-net

volumes:
  redis_data:

networks:
  traefik-net:
    name: traefik-net
    external: true
